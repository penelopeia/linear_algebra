def trace(m):
    return m

# requires the determinant complete
def inverse(m):
    return m

# m is a square matrix
def determinant(m):
    # multiply the first element of each row by the determinant of the
    # remaining row elements? recursive is best?
    # for i, row in enumerate(m):
    #     row[0] * (m)
    return m

def eigenvalue():
    return True

def eigenvector():
    return True
